import React from 'react'
import classes from './styles.module.css'

function ButtonSlider({text}) {
  return (
    <div className={classes.buttonAnterior} onClick={()=>{}}>{text}</div>
  )
}

export default ButtonSlider